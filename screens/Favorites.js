import { StyleSheet, Text } from "react-native";

function Favorites() {
  return <Text style={styles.text}>The favorites screen. Start working on this.</Text>;
}

export default Favorites;

const styles = StyleSheet.create({
  text: {
    color: "white",
    margin: 2,
  },
});
